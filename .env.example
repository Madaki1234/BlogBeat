# Environment Configuration

# Application Environment
# Set to 'production' in production environments
NODE_ENV=development

# MongoDB Connection String
# Required in production
# Example: mongodb+srv://username:password@cluster.mongodb.net/blog
MONGODB_URI=

# Session Secret
# IMPORTANT: Change this in production!
SESSION_SECRET=dev-blog-secret-key

# Server Settings
PORT=5000

# Optional JWT Secret for token-based auth (if implemented)
JWT_SECRET=dev-jwt-secret-key 

# Other Settings
# MAX_UPLOAD_SIZE=5 # Max upload size in MB
# ENABLE_ANALYTICS=false